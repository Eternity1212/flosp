# ---------------------------------------------------------------------------
# 数据与权重：绝对不要提交。
# 这些文件动辄几十 GB，而且 Messidor-2 / RETFound 的许可都禁止再分发。
#
# 注意开头的斜杠：只忽略**仓库根目录**下的 data/，不能写成 `data/`，
# 否则会把源码里的 fedosp/data/（数据管线代码）一起忽略掉 —— 那样克隆下来的
# 仓库根本跑不起来。这个坑真的踩过，别改回去。
# ---------------------------------------------------------------------------
/data/
/weights/
*.pth
*.pt
*.ckpt
*.safetensors
*.zip
*.tar
*.tar.gz
*.h5

# 数据集图像
*.jpeg
*.jpg
*.png
*.tif
*.tiff
# 但论文插图要留（用 -f 强制加过的除外）
!figures/*.png
!docs/**/*.png

# ---------------------------------------------------------------------------
# 实验产物（同样只忽略仓库根目录下的，避免误伤源码目录）
# ---------------------------------------------------------------------------
/runs/
/logs/
/outputs/
/wandb/
/tables/
/figures/
manifest*.csv
!configs/experiment_matrix.csv

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
eggs/
*.egg-info/
.installed.cfg
*.egg
.eggs/

# 虚拟环境
venv/
.venv/
env/
ENV/
.conda/

# 测试与类型检查
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/
.dmypy.json

# ---------------------------------------------------------------------------
# 编辑器与操作系统
# ---------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/
*.ipynb_convert*

# ---------------------------------------------------------------------------
# 凭据：一旦提交就要立刻吊销，别赌
# ---------------------------------------------------------------------------
kaggle.json
.kaggle/
*.env
.env
.env.*
*token*
!**/tokenizer*
huggingface_token*
